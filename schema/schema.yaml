# NeuroBlueprint metadata schema
# https://github.com/neuroinformatics-unit/NeuroBlueprint

version: "0.1.0"

# Valid top-level sections in a metadata.yaml file.
# Each entry maps to a schema definition file in this directory.
sections:
  project: project.yaml
  sub: sub.yaml
  ses: ses.yaml
  ephys: ephys.yaml
  behav: behav.yaml
  anat: anat.yaml
