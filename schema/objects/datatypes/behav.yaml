# Behavioural experiments datatype metadata fields.
#
# These fields appear under the "behav" section of metadata.yaml and
# describe behavioural task acquisitions.
#
# Based on the BIDS Behavioural Experiments specification:
# https://bids-specification.readthedocs.io/en/stable/modality-specific-files/behavioral-experiments.html

# ---- Institution Information ------------------------------------------------

InstitutionName:
  name: InstitutionName
  display_name: Institution Name
  description: |
    The name of the institution where the data were collected.
  type: string
  requirement_level: recommended

InstitutionAddress:
  name: InstitutionAddress
  display_name: Institution Address
  description: |
    The address of the institution where the data were collected.
  type: string
  requirement_level: optional

InstitutionalDepartmentName:
  name: InstitutionalDepartmentName
  display_name: Institutional Department Name
  description: |
    The name of the department within the institution where the
    data were collected.
  type: string
  requirement_level: optional

# ---- Task Information -------------------------------------------------------

TaskName:
  name: TaskName
  display_name: Task Name
  description: |
    Name of the task performed during the recording
    (e.g. "Visual Decision Making").
  type: string
  requirement_level: recommended

TaskDescription:
  name: TaskDescription
  display_name: Task Description
  description: |
    Longer free-text description of the task, including the
    structure, stimuli, and response modality.
  type: string
  requirement_level: optional

Instructions:
  name: Instructions
  display_name: Instructions
  description: |
    Task instructions given to the subject.
    Use "n/a" if not applicable (e.g. for animal subjects).
  type: string
  requirement_level: optional

CogAtlasID:
  name: CogAtlasID
  display_name: Cognitive Atlas ID
  description: |
    URL of the Cognitive Atlas entry for the task
    (https://www.cognitiveatlas.org/).
  type: string
  format: uri
  requirement_level: optional

CogPOID:
  name: CogPOID
  display_name: CogPO ID
  description: |
    URL of the CogPO (Cognitive Paradigm Ontology) entry for the task.
  type: string
  format: uri
  requirement_level: optional
